import React from 'react';
import PropTypes from 'prop-types';
import styles from './ResponseBar.module.css';

const ResponseBar = () => (
  <div className={styles.ResponseBar}>
    ResponseBar Component
  </div>
);

ResponseBar.propTypes = {};

ResponseBar.defaultProps = {};

export default ResponseBar;
